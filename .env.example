# Frontend only configuration (Optional -- defaults are provided in compose.yaml).
# The master stack is started from cem-master-backend/.env.

# Port the public map site is served on: http://localhost:8000
FRONTEND_PORT=8000
